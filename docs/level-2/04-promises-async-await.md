# 04 · Promises & Async/Await

!!! info "Coming soon"
    This module's full lessons and code snippets are being written next.
    Creating and chaining Promises, `async`/`await` syntax, and error handling
    with `try`/`catch` around asynchronous code.

Check the task ledger or ask your virtual employee for a status update on
remaining content.
